<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="../public/styles/main.css" type="text/css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Girls</title>
</head>
<body>
    <h3><%= Girl %></h3>
    <div class="galleryHolder">
        <% let tN = 0 %>
        <% Thumbs.forEach(item => { %>
            <div class="galleryItem">
                <p class="txtGal"><%= item.split('/')[3] %></p>
                <img class="gallerieThumb" src="/<%= item %>" onclick="javascript:window.location='/girls/<%= Girl+'/'+item %>'">
            </div>
        <% tN = tN + 1 %>
        <% }); %>
    </div>
</body>
</html>
